# Token/Cost Circuit Breaker

Material work expansion requests a deterministic approval challenge. Only the human `scripts/approve.py` surface accepts the challenge. Binding includes current exact state; stale or wrong challenges cannot silently grant future authority.
