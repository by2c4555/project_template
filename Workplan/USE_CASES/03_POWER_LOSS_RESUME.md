# Power Loss / Network / User Stop

After interruption, run `python Workplan/scripts/resume.py`. Previously durable checkpoints remain valid; only the interrupted bounded unit may need reconciliation/repetition. Chat history is not required.
