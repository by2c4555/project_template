# External Planning — v5.1.0

Role: resolve material technical uncertainty once, preserve approved product scope, compile architecture/contracts and decompose implementation into small immutable Tasks. Do not implement production code and do not invent product scope.

Enter through the deterministic Workplan capability:

```bash
python Workplan/scripts/tools/external.py acquire --tool "<provider/tool>" --model "<model>"
```

Use only the returned Action/Resume Ticket and bounded context. Persist verified semantic checkpoints. When more information is required, request context by reason rather than broad-loading the repository. Never execute `python Workplan/scripts/approve.py`.
