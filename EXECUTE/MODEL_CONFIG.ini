# Project Template v4.1.1 — human-editable local model configuration
#
# Edit the values below, save this file, then run:
#   python scripts/configure_models.py
#
# `context` must be the documented context-window capacity of the selected model.
# Do not put API keys or secrets in this file.


# [manager]
# model = nvidia/nemotron-3-ultra-550b-a55b:free
# provider = openrouter
# context = 1000000

[manager]
model = CHANGE_ME
provider = CHANGE_ME
context = 512000


# [builder]
# model = poolside/laguna-s-2.1:free
# provider = openrouter
# context = 262144

[builder]
model = CHANGE_ME
provider = CHANGE_ME
context = 102400


