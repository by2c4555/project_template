# START NEXT VERSION / FEATURE RESEARCH

Run Project Template v4.3.0 research in `NEXT_VERSION_RESEARCH` mode using `MASTER_RESEARCH_PROMPT.md`.

Required baseline input:
- latest verified `EXECUTE/evaluation/PROJECT_COMPLETION_REPORT_Vx.md`

Also use:
- the user's new feature/version intent;
- current `EXECUTE/project_details.md` / Research history when useful;
- relevant new domain/external evidence.

First understand the completed system as it actually exists. Then define the new/changed product scope while preserving relevant invariants and compatibility constraints.

Create the next immutable `Research_Vx` and update current scope artifacts.

Do not diagnose prior implementation incidents; use their verified lessons only as baseline constraints when relevant.
Do not create the implementation plan.
